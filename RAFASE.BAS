CLS
REM"calculadora,CAMBIO DE DOLAR,PROMEDIO,JUEGO"
ELECCION:
INPUT "QUE DESEA HACER JUGAR,PROMEDIO,CAMBIO DE DOLAR,CALCULADORA", Q$
IF Q$ = "JUGAR" THEN GOTO JUGAR ELSE GOTO 45
45
IF Q$ = "PROMEDIO" THEN GOTO PROMEDIO ELSE GOTO 56
56
IF Q$ = "CALCULADORA" THEN GOTO ENTRAR ELSE GOTO DOLAR
PROMEDIO:
INPUT "CUAL ES TU NOMBRE", L$
INPUT "CUAL ES TU APELLIDO", P$
INPUT "DIGITE LAS 4 NOTAS", A, B, C, D
INPUT "DIGITE LA NOTA DEL EXAMEN", Y
Q = (A + B + C + D) / 4
W = Q * .7
E = Y * .3
T = W + E
PRINT L$, P$, "TU PROMEDIO ES:", Q
PRINT L$, P$, "TU 70% DEL PROMEDIO ES:", W
PRINT L$, P$, "TU 30% DEL EXAMEN ES:", E
PRINT L$, P$, "TU NOTA FINAL ES:", T
IF T >= 70 THEN PRINT "FELICITACIONES APROBASTE" ELSE GOTO QUE
GOTO HO
QUE:
PRINT "LAMENTABLEMENTE REPROBASTE"
HO:
INPUT "DESEA CONTINUAR CON EL PROMEDIO SI O NO", W$
IF W$ = "SI" THEN IF T <= 69 GOTO 444 ELSE IF W$ = "SI" THEN IF T >= 70 GOTO OPCION ELSE GOTO 555
555
IF W$ = "NO" THEN GOTO OPCION ELSE GOTO HO
444
INPUT "DIGITA LA NOTA DEL EXAMEN COMPLETIVO", I
Z = I * .5
C = Q * .5
X = Z + C
PRINT L$, P$, "TU 50% DEL EXAMEN COMPLETIVO ES:", Z
PRINT L$, P$, "TU 50% DEL PROMEDIO ES:", C
PRINT L$, P$, "TU NOTA FINAL ES:", X
IF X > 70 THEN PRINT "YA ERA JUSTO" ELSE GOTO QUEM
GOTO JOK
QUEM:
PRINT "TE PELATE DEBES ESTUDIAR MUCHO PARA ESTE EXAMEN O REPETIRAS CURSO"
JOK:
INPUT "DESEA CONTINUAR CON EL PROMEDIO SI O NO", A$
IF A$ = "SI" THEN IF X <= 69 GOTO 78 ELSE IF A$ = "SI" THEN IF X >= 70 GOTO OPCION ELSE GOTO 39
39
IF A$ = "NO" THEN GOTO OPCION ELSE GOTO QUEM
78
INPUT "DIGITE LA NOTA DEL EXAMEN EXTRAORDINARIO", V
N = Q * .3
O = V * .7
A = N + O
PRINT L$, P$, "TU NOTA DEL 30% DEL PROMEDIO ES:", N
PRINT L$, P$, "TU NOTA DEL 70% DEL PROMEDIO ES:", O
PRINT L$, P$, "TU NOTA FINAL ES:", A
IF A > 70 THEN PRINT "ERES DICHOSO" ELSE PRINT "MARDITO ESTUPIDO REPETIRAS DE CURSO JAJAJAJA "
GOTO OPCION
JUGAR:
PRINT "ADIVINA CUALES SON LOS NUMEROS QUE SUMANDO LOS DOS PRIMERO SE DIVIDA CON EL PRODUCTO DE LA MULTIPLICACION DE LOS DOS ULTIMOS"
23
INPUT "DUGITA LOS 4 VALORES", A, B, C, D
X = (A + B) / (C * D)
PRINT X
IF X <> 8 THEN GOTO OPCION ELSE INPUT "TE RINDES S/N", H$
4545
IF H$ = "S" THEN GOTO OPCION ELSE GOTO 22
22
IF H$ = "N" THEN GOTO 23 ELSE GOTO 4545
DOLAR:
IF Q$ = "CAMBIO DE DOLAR" THEN GOTO CAMBIO ELSE GOTO ELECCION
CAMBIO:
INPUT "ENTRE LA TASA DEL DOLAR", A
INPUT "ENTRE LA CANTIDAD DE PESOS", B
INPUT "ENTRE LA CANTIDAD DE DOLARES", C
INPUT "SI QUIERE CAMBIAR DE DOLAR A PESO PULSE D Y DE LO CONTRARIO P", F$
X = C * A
H = B / A
IF F$ = "D" THEN PRINT "LA CANTIDAD DE PESOS SON:", X ELSE GOTO AJO
AJO:
IF F$ = "P" THEN PRINT "LA CANTIDAD DE DOLARES SON:", H ELSE GOTO OPCION
ENTRAR:
INPUT "si DESEA hacer operaciones con dos numeros digite D,CON TRES T,CON CUATRO DIGITE C,CON CINCO S", N$
IF N$ = "D" THEN GOTO 2 ELSE GOTO DOS
DOS:
IF N$ = "T" THEN GOTO 3 ELSE GOTO TRES
TRES:
IF N$ = "C" THEN GOTO 4 ELSE GOTO CINCO
CINCO:
IF N$ = "S" THEN GOTO 5 ELSE GOTO ENTRAR
2
INPUT "DIGITE LOS DOS VALORES", C, D
ENTRAR2:
INPUT "SI QUIERE SUMAR PULSE S,RESTAR R, MULTIPLICAR M DIVIDIR D", M$
Q = C + D
W = C - D
E = C * D
T = C / D
IF M$ = "S" THEN PRINT Q ELSE GOTO RESTA
GOTO OPCION
RESTA:
IF M$ = "R" THEN PRINT W ELSE GOTO MULTIPLICACION
GOTO OPCION
MULTIPLICACION:
IF M$ = "M" THEN PRINT E ELSE GOTO DIVISION
DIVISION:
IF M$ = "D" THEN PRINT T ELSE GOTO ENTRAR2
GOTO OPCION
3
INPUT "DIGITE LOS TRES VALORES", D, E, F
ENTRADA:
INPUT "SI QUIERE SUMAR PULSE SU,RESTAR RE,MULTIPLICAR MU", B$
P = D + E + F
O = D - E - F
I = D * E * F
IF B$ = "SU" THEN PRINT P ELSE GOTO RES
GOTO OPCION
RES:
IF B$ = "RE" THEN PRINT O ELSE GOTO MUL
GOTO OPCION
MUL:
IF B$ = "MU" THEN PRINT I ELSE GOTO ENTRADA
GOTO OPCION
4
INPUT "DIGITE LOS CUATRO VALORES", G, H, I, J
ENTRADA1:
INPUT "SI QUIERE SUMAR PULSE SUM,RESTAR REST,MULTIPLICAR MULT", V$
L = G + H + I + J
K = G - H - I - J
J = G * H * I * J
IF V$ = "SUM" THEN PRINT L ELSE GOTO RESTAS
GOTO OPCION
RESTAS:
IF V$ = "REST" THEN PRINT K ELSE GOTO MULTIPLICACIONES
GOTO OPCION
MULTIPLICACIONES:
IF V$ = "MULT" THEN PRINT J ELSE GOTO ENTRADA1
GOTO OPCION
5
INPUT "DIGITE LOS CINCO VALORES", A, B, C, D, E
ENTRADA5:
INPUT "SI QUIERE SUMAR PULSE SUMA,RESTAR RESTA,MULTIPLICAR MULTI", C$
Q = A + B + C + D + E
S = A - B - C - D - E
D = A * B * C * D * E
IF C$ = "SUMA" THEN PRINT Q ELSE GOTO LAO
GOTO OPCION
LAO:
IF C$ = "RESTA" THEN PRINT S ELSE GOTO LOA
GOTO OPCION
LOA:
IF C$ = "MULTI" THEN PRINT D ELSE GOTO ENTRADA5
GOTO OPCION
OPCION:
INPUT "SI QUIERE CONTINUAR PULSE si DE LO CONTRARIO PULSE NO", L$
IF L$ = "SI" THEN GOTO ELECCION ELSE GOTO OPCION2
OPCION2:
IF L$ = "NO" THEN GOTO finalizar ELSE GOTO OPCION
finalizar:
PRINT "CREADO POR RAFAEL J RODRIGUEZ, RAFASE_282@HOTMAIL.COM,3C,No. 15,(R.282)Y/O (RAFASE_282),CUALQUIER INQUIETUD COMUNICARSE AL EMAIL O AL 560-3989"
END





